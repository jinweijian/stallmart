package com.stallmart.store.internal.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface StoreDecorationRepository extends JpaRepository<StoreDecorationEntity, Long> {
}
