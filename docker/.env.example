# ================================================
# StallMart 本地开发环境配置
# ================================================
# 注意：禁止将任何服务暴露到公网！
# 本地开发环境，仅限 localhost 访问
# ================================================

# Java 后端
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080

# MySQL
MYSQL_VERSION=9.0
MYSQL_ROOT_PASSWORD=stallmart_dev_root
MYSQL_DATABASE=stallmart
MYSQL_USER=stallmart
MYSQL_PASSWORD=stallmart_dev_pass
# 仅监听本地端口，不暴露到外网
MYSQL_PORT=3307

# Redis
REDIS_VERSION=7.2-alpine
REDIS_PASSWORD=stallmart_dev_redis
# 仅监听本地端口，不暴露到外网，避开本机常见 Redis 6379
REDIS_PORT=6380

# JWT 密钥（生产环境必须修改！）
JWT_SECRET=your-256-bit-secret-key-here-change-in-production-min-32-chars
JWT_ACCESS_TOKEN_VALIDITY=7200
JWT_REFRESH_TOKEN_VALIDITY=604800
STALLMART_CORS_ALLOWED_ORIGINS=http://localhost:10086,http://127.0.0.1:10086

# 阿里云 OSS（可选，本地开发可注释）
# OSS_ACCESS_KEY_ID=your_access_key
# OSS_ACCESS_KEY_SECRET=your_secret_key
# OSS_BUCKET=stallmart
# OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com

# 微信小程序（开发环境使用测试号或本地配置）
WECHAT_APP_ID=your_wechat_app_id
WECHAT_APP_SECRET=your_wechat_app_secret

# App H5 Docker 联调
TARO_APP_ID=wx-stallmart-demo
TARO_APP_API_BASE_URL=http://localhost:8081/api/v1
TARO_APP_H5_PORT=10086
